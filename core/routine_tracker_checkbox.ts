export class RoutineTrackerCheckbox {
  private dailyTotal: number;
  private weeklyTotal: number;
  private monthlyTotal: number;
  private yearlyTotal: number;
  private allTimeTotal: number;

  createTaskTracker() {
    
  }
}
